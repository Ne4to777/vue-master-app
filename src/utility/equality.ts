export const equal = (x: any) => (y: any): boolean => x === y
export const unequal = (x: any) => (y: any): boolean => x !== y
export const gt = (x: any) => (y: any): boolean => x > y
export const lt = (x: any) => (y: any): boolean => x < y
export const gte = (x: any) => (y: any): boolean => x >= y
export const lte = (x: any) => (y: any): boolean => x <= y
